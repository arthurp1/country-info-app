var fs = require('fs')

function parse (file, callback) {
	fs.readFile(file, 'utf-8', function(err, data){
		var obj = {}
	    if (err) throw err;
	    jsonData = JSON.parse(data); //JSON Parse creates an obect
	    callback(jsonData);
	});

}

 module.exports = parse